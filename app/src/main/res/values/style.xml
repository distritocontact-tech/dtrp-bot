<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="tab_layout_title" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:fontFamily">@font/montserrat_bold</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- Evita a "piscada branca" ao trocar de Activity: sem isso, o
             fundo padrão claro do AppCompat.Light aparece por um instante
             antes do conteúdo da tela (que é sempre escuro) ser desenhado. -->
        <item name="android:windowBackground">@android:color/black</item>
        <!-- Também evita o flash: desliga a janela de transição padrão do
             Android nessa troca, já que quem cuida da animação agora é o
             ManualScreenAnimator (ver BaseLauncherActivity). -->
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="CustomDialogTheme" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>